# Fold 7 真机验证清单

工程路径：`C:\Users\jszha\Downloads\ledger`
Debug APK：`app\build\outputs\apk\debug\app-debug.apk`

## 已在本机完成

- [x] `./gradlew :app:assembleDebug` 成功
- [x] `:domain:test` / `:data:test`（金额解析、指纹去重）通过
- [x] 真机 `SM_F966U`（Fold）安装 Debug 包并启动 `MainActivity` 成功
- [x] 前台保活通知已触发；启动无 FATAL 崩溃
- [x] 内屏分辨率采样：`1968x2184`

## 请你本人再验收（需真实付款）

### 安装与权限

1. `adb install -r app\build\outputs\apk\debug\app-debug.apk`
2. 完成引导：通知使用权、无障碍、悬浮窗、忽略电池优化
3. 设置 → 启动保活前台服务；最近任务中锁定 FoldLedger
4. 应用详情 → 允许后台活动 / 自启动（One UI）

### 自动记账

5. 微信支付一笔小额 → 应出现不侵入状态栏的限宽悬浮卡片。
6. 点“稍后”仅关闭卡片，流水页仍保留待确认；点“打开应用核对”可修改金额、商户、账户和分类。
7. 点“确认记账”后流水可见；对任意流水执行删除后，可通过底部提示立即撤销。
6. 支付宝扫码/碰一碰 → 通知或成功页识别 → 入库且不与通知重复
7. 连续同金额同商户 2 分钟内 → 不应重复记账
8. 设置开启「全自动入库」后再付一笔 → 无需确认直接入库

### Fold UI

9. 外屏：单栏流水可用
10. 内屏展开：左列表右详情
11. 折叠/展开切换不丢列表位置

### 产品功能

12. 手动记账（支出/收入/转账）
13. 账户与分类新增/归档；关键词自动归类
14. 本月预算与超支提示
15. 统计页按日柱状 + 分类占比
16. 导出 CSV / JSON 备份可分享

### 保活对抗

17. 强制停止后重新打开，通知监听状态提示正确
18. 锁屏闲置数小时后付款，仍能捕获（或至少「监听未就绪」醒目提示）
